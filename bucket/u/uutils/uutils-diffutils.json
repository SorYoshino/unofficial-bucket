{
    "version": "0.5.0",
    "description": "A cross-platform Rust reimplementation of GNU diffutils (binaries compiled with MSVC), with some options or behaviors potentially differing from GNU diffutils.",
    "homepage": "https://uutils.org/diffutils/",
    "license": "MIT|Apache-2.0",
    "suggest": {
        "Microsoft Visual C++ 2015-2022 Redistributable": "extras/vcredist2022"
    },
    "architecture": {
        "64bit": {
            "url": "https://github.com/uutils/diffutils/releases/download/v0.5.0/diffutils-x86_64-pc-windows-msvc.zip",
            "hash": "36c900a06060ee31621fcdecb9bf223b40dd23d45e91ba98101b5d3aa5c70d18"
        }
    },
    "pre_install": [
        "$current_depth = 0",
        "while ($current_depth -lt 5) {",
        "    $items = Get-ChildItem -Path $dir | Select-Object -First 3",
        "    if ($items.Count -eq 1 -and $items.PSIsContainer) {",
        "        $inner_dir = $items | ForEach-Object -MemberName FullName",
        "        Move-Item -Path \"$inner_dir\\*\" -Destination $dir -Force",
        "        Remove-Item -Path $inner_dir -Recurse -Force -ErrorAction SilentlyContinue",
        "        $current_depth++",
        "    } else { break }",
        "}"
    ],
    "bin": [
        "diffutils.exe",
        [
            "diffutils.exe",
            "cmp",
            "cmp"
        ],
        [
            "diffutils.exe",
            "diff",
            "diff"
        ]
    ],
    "checkver": {
        "github": "https://api.github.com/repos/uutils/diffutils/releases/latest"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://github.com/uutils/diffutils/releases/download/v$version/diffutils-x86_64-pc-windows-msvc.zip"
            }
        },
        "hash": {
            "url": "$baseurl/sha256.sum"
        }
    }
}
